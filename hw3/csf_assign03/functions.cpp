#include "functions.h"

Cache cache create_cache(int numSets, int numBlocks, int bytesOfMemory){
     for (int i = 0; i < numSets; i++){
        for (int j = 0; j < numBlocks; j++){
            
        }
    }
}